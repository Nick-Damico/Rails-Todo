// Clear new list item input field
$('#list_item_name').val('');

// Appends List Item into the tablebody
$('tbody').append('<%= j render partial: "list-item", locals: {list: @list, item: @list_item} %>');
